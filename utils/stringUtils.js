// 3) =====================================================
// ============ Re-exporting Export =======================
// ========================================================

const strLength = (str) => {
    return str.length;
};

export { strLength };